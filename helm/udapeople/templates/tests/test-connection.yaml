apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "udapeople.fullname" . }}-test-connection"
  labels:
    {{- include "udapeople.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "udapeople.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
